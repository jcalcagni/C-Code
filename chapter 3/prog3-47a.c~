#include <stdio.h>

main()
{

	int number = 0, sum = 0, remainder = 0, fact = 1;

	printf ("Enter a number: ");
	scanf ("%d", &number);

/* 	while (number > 0) {
	   remainder = number % 10;
	   sum = sum + remainder;
	   number = number / 10;

	} */

	
	while (number != 0) {
	   
	   fact = fact * number;
	   number--;

	}

/*	printf ("Sum of the digits is: %d\n", sum);  */

	printf ("factorial is: %d\n", fact);   
	
	
	

	return 0;
}

